import { Button } from "antd";

export default function Dashboard() {
  return (
    <div>
      <Button className="text-primary" type="primary">
        Test button
      </Button>
    </div>
  );
}
